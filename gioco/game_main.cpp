#include "decisions.hpp"
#include "events.hpp"
#include <string> 

int main() {
    
    std::cout << "basic welcome message + some commands and rules"
    City active_city;

    while (true) {
        // selection of city
        break;
    } 
    
    while (true) {
        
        std::string u_input;
        std::cin >> u_input;

        if (u_input == "lockdown") {
            std::cout << "you selected lockdown \n";
        };

        break;
    }
}